<p> i used git commands like</p>
<br>
<p> first i amde a folder then creted a html and css file, then </p>
<br>
<p> i used " git init" to make the folder a git repo </p>
<br>
<p>'git remote add origin "link of my repo" ' to  add the folder in my repo and set the origin</p>
<br>
<p> git remote -v  to verify remote</p>
<br>
<p> git branch  to check the branch</p>
<br>
<p>git push origin main   to push the project in my repo</p>
<br>
<p>  now i added a readme file and then</p>
<br>
<p>used  git add .  to save all the changes in files</p>
<br>
<p>git status, to check the status</p>
<br>
<p>git commit -m "msg"  to commit changes </p>
<br>
<p>git push origin main  tp push the changes in the repo</p>

